#### first step after install
```
su
apt install sudo
usermod -a -G sudo best
exit
```


sudo apt install git

git clone https://github.com/bestuzheff/dotfiles

cd dotfiles

./install.sh
./nerdfonts.sh